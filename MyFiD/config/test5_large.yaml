# basic parameters
checkpoint_dir: 'checkpoint'
model_path: 'checkpoint/baseline5_large/checkpoint/best_dev'
name: 'baseline5_large_test'
gpu: 1
per_gpu_batch_size: 4
seed: 0

# evaluation parameters
eval_data: 'data/test.json'
write_results: true

# model specific parameters
n_context: 5
text_maxlength: 250
no_sent: false
no_path: false
duplicate_sample: true